# SteamASCIIGen — Claude Code entry

All rules live in **[AGENTS.md](./AGENTS.md)** — read it in full first. This is a **dormant
app-track scaffold** (Svelte 5 + Vite + Tailwind): the `docs/` session chain isn't built yet —
scaffold it from the OGDK app-track template when you resume real work. Build check: `npm run build`.
