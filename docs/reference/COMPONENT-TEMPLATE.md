# <Component Name>

> One-sentence summary: what it is and the problem it solves.
> **Since:** v<X.Y> · **Source:** `<path/to/module>` · **Spec:** `core/<spec>.md` · **Origin plan:** `plans/archive/<PLAN>.md`

## Overview

Two or three paragraphs for a reader with zero project context: what this component
does, when to reach for it, and what it deliberately does NOT do (scope boundaries).

## Quick start

The minimum working example — copy-paste runnable. Show the most common use in under
ten lines, with expected output.

```
<smallest possible working example>
```

## API / Usage

Every public function/class/entry point, one block each:

### `function_name(args) -> return`
What it does. Argument meanings, units, and valid ranges. Return value. Errors raised
and when. One example if non-obvious.

## Behavior & guarantees

The contracts a consumer can rely on: invariants held, precision/units, ordering
guarantees, thread/process assumptions, performance characteristics (and budgets if
applicable). If a guarantee is verified by a test, name the test.

## Gotchas & limits

The mistakes a reasonable person will make, and the boundaries where behavior
degrades. Be specific — this section is why the page exists. Include the hard-won
lessons (link `[[memory]]`-grade incidents recorded in STATUS/plans).

## Related

- Other reference pages this composes with
- The core spec for design rationale

## Changelog

| Version | Date | Change |
|---------|------|--------|
| <X.Y> | <date> | Initial reference page |
